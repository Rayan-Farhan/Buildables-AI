# Week6_lesson — Example Flask and FastAPI apps

This directory contains three small example apps used for Week 6:

- `T1/` — a simple Flask app (`flask_app.py`)
- `T2/` — a FastAPI example (`fastapi_app.py`)
- `T3/` — extended FastAPI with auth and delete functionalities (`fastapi_app.py`)